import { StyledContainer, StyledSection, StyledTitle } from "components/styled";

function AddIngredient() {
    return (
        <StyledContainer>
            <StyledSection>
                <StyledTitle>재료추가</StyledTitle>
            </StyledSection>
        </StyledContainer>
    )
}

export default AddIngredient;